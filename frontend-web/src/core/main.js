import { createApp } from 'vue'
import App from './App.vue'
import './style.css'

// Monta la app en el elemento con id `#app` (ver index.html)
// Esto es lo que arranca la interfaz en el navegador.
createApp(App).mount('#app')
